{
  "name": "@magictrust/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "api-client:create": "node scripts/create-api-client.mjs",
    "admin:user:create": "node scripts/create-admin-user.mjs",
    "admin:user:set-password": "node scripts/set-admin-password.mjs",
    "pii:backfill": "node scripts/pii-backfill.mjs",
    "webhook:create": "node scripts/create-webhook.mjs",
    "webhook:deliver": "node scripts/deliver-webhooks.mjs"
  },
  "dependencies": {
    "@magictrust/config": "workspace:*",
    "@magictrust/domain": "workspace:*",
    "@magictrust/privacy": "workspace:*",
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
